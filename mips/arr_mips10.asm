# ??t giá tr? s? l??ng ph?n t? c?a m?ng vào $s0
# ??t ??a ch? ??u tiên c?a m?ng vào $s1
# ??t con tr? ?? truy xu?t ph?n t? cu?i cùng c?a m?ng vào $s2
# ??t bi?n t?m ?? trao ??i giá tr? 2 ph?n t? vào $s3

li $t0, 0             # kh?i t?o bi?n ??m s? ph?n t? d??ng
move $s1, $a0         # l?u ??a ch? ??u tiên c?a m?ng vào $s1
add $s2, $a0, $a1     # l?u con tr? ?? truy xu?t ph?n t? cu?i cùng c?a m?ng vào $s2
addi $s2, $s2, -4     # tr? 4 ?? truy xu?t ??n ph?n t? cu?i cùng c?a m?ng

loop:
    bge $s1, $s2, end # n?u con tr? trái >= con tr? ph?i thì k?t thúc

    lw $t1, ($s1)     # t?i giá tr? c?a ph?n t? tr? b?i con tr? trái vào $t1
    lw $t2, ($s2)     # t?i giá tr? c?a ph?n t? tr? b?i con tr? ph?i vào $t2

    blez $t1, swap    # n?u ph?n t? trái là s? âm ho?c 0 thì swap
    bgtz $t2, swap    # n?u ph?n t? ph?i là s? d??ng thì không c?n swap

    addi $s1, $s1, 4  # t?ng con tr? trái
    addi $s2, $s2, -4 # gi?m con tr? ph?i
    j loop            # quay l?i loop

swap:
    sw $t1, ($s2)     # ghi giá tr? c?a ph?n t? trái vào ph?n t? tr? b?i con tr? ph?i
    sw $t2, ($s1)     # ghi giá tr? c?a ph?n t? ph?i vào ph?n t? tr? b?i con tr? trái
    addi $s1, $s1, 4  # t?ng con tr? trái
    addi $s2, $s2, -4 # gi?m con tr? ph?i
    addi $t0, $t0, 1  # t?ng bi?n ??m s? ph?n t? d??ng
    j loop            # quay l?i loop

end:
